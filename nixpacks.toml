[phases.setup]
aptPkgs = ["libopus-dev", "libsodium-dev", "ffmpeg", "python3", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt"
]

[phases.start]
cmd = "python main.py"